# 🦞 CS2 BP 分析网站 — 第三层：深度分析设计文档

> **版本**：v1.0  
> **日期**：2026-06-05  
> **作者**：小龙虾团队 — 数据分析师（扇贝 🐚）  
> **关联项目**：CS2 职业比赛 BP 策略分析网站  
> **当前 Active Duty 七图**：Ancient、Dust2、Inferno、Mirage、Nuke、Anubis、Overpass

---

## 目录

1. [地图风格聚类分析](#1-地图风格聚类分析)
2. [BP 胜率预测模型](#2-bp-胜率预测模型)
3. [教练/分析师 BP 风格画像](#3-教练分析师-bp-风格画像)
4. [版本更新影响分析](#4-版本更新影响分析)
5. [数据管道与工程架构](#5-数据管道与工程架构)
6. [MVP 路线图与迭代路径](#6-mvp-路线图与迭代路径)
7. [附录：特征工程参考表](#7-附录特征工程参考表)

---

## 1. 地图风格聚类分析

### 1.1 目标

将七张 Active Duty 地图基于多维属性进行无监督聚类，回答以下问题：
- 哪些地图风格相似、可以在 BP 中互相替代？
- 哪些地图是"独特"的、不可替代的？
- 地图之间的"距离"如何量化？

### 1.2 特征向量设计

每张地图用一个 **14 维特征向量** 表示：

| 类别 | 特征 | 说明 | 数据来源 |
|------|------|------|----------|
| **胜率** | CT 胜率 | 职业比赛中 CT 方平均胜率 | HLTV / Leetify |
| **胜率** | T 胜率 | 职业比赛中 T 方平均胜率 | HLTV / Leetify |
| **胜率** | 均衡度 | \|CT% - T%\|，越小越均衡 | 计算值 |
| **地图节奏** | 平均回合时长 | 职业比赛平均回合时长（秒） | HLTV |
| **地图节奏** | 手枪局胜率差 | CT vs T 手枪局胜率差 | HLTV |
| **空间结构** | 关键控制区数量 | 地图上的关键 choke point 数量 | 专家标注 |
| **空间结构** | 回防难度指数 | 从 CT spawn 到 A/B 的平均时间 | 地图分析 |
| **空间结构** | 狙击权重 | 长枪位/对枪位的占比 | 专家评分 1-10 |
| **空间结构** | 道具权重 | 烟雾/闪光关键投掷位数量 | 专家评分 1-10 |
| **空间结构** | 垂直维度 | 是否有上下层结构（0/1） | 地图属性 |
| **比赛数据** | 禁选率 | 该图在职业 BP 中被 ban 的频率 | HLTV |
| **比赛数据** | 选图率 | 该图在职业 BP 中被 pick 的频率 | HLTV |
| **比赛数据** | 胜率方差 | 不同战队在该图上的胜率标准差 | HLTV |
| **比赛数据** | 爆冷率 | 排名差距大的队伍在该图上的意外结果率 | HLTV |

### 1.3 七图初始特征估计（基于 2025-2026 公开数据）

| 地图 | CT% | T% | 均衡度 | 倾向 | 狙击权重 | 道具权重 | 垂直 | 关键控制区 |
|------|-----|-----|--------|------|----------|----------|------|-----------|
| Ancient | 50.8% | 49.2% | 1.6 | 微 CT | 7 | 7 | 0 | 3 (mid, A ramp, B) |
| Dust2 | 49.1% | 50.9% | 1.8 | 微 T | 9 | 5 | 0 | 3 (mid, long A, B tunnels) |
| Inferno | 49.0% | 51.0% | 2.0 | 微 T | 5 | 9 | 0 | 3 (mid, banana, A) |
| Mirage | 54.2% | 45.8% | 8.4 | 强 CT | 8 | 7 | 0 | 3 (mid, A ramp, B apts) |
| Nuke | 55.2% | 44.8% | 10.4 | 强 CT | 7 | 8 | 1 | 4 (ramp, secret, A main, yard) |
| Anubis | 43.3% | 56.7% | 13.4 | 强 T | 6 | 8 | 0 | 3 (mid, A, B) |
| Overpass | 56.4% | 43.6% | 12.8 | 强 CT | 8 | 7 | 0 | 3 (bathroom, connector, B) |

> **注**：以上数据综合自 Skinrave、BitSkins、Leetify 等公开来源，实际部署时需通过 HLTV API 获取最新数据。

### 1.4 聚类方法

#### MVP 方案：K-means (k=3)

```
聚类 1: "CT 堡垒" — Nuke, Overpass, Mirage
  → 强 CT 倾向，回防快，CT 控制区域大
  → BP 含义：pick 这些图时 CT 方有天然优势

聚类 2: "T 快攻" — Anubis, Inferno
  → T 方有优势，快攻路线清晰
  → BP 含义：pick 这些图时 T 方可以主动施压

聚类 3: "均衡战场" — Ancient, Dust2
  → 双方胜率接近，更依赖个人能力和战术执行
  → BP 含义：适合实力接近的对决，减少地图因素
```

#### 进阶方案：层次聚类 + PCA 可视化

```python
# 伪代码
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA
from sklearn.cluster import AgglomerativeClustering
import scipy.cluster.hierarchy as sch

# 1. 标准化
scaler = StandardScaler()
X_scaled = scaler.fit_transform(map_features_df)

# 2. PCA 降维到 2D 用于可视化
pca = PCA(n_components=2)
X_pca = pca.fit_transform(X_scaled)

# 3. 层次聚类
clustering = AgglomerativeClustering(n_clusters=3, linkage='ward')
clusters = clustering.fit_predict(X_scaled)

# 4. 树状图
dendrogram = sch.dendrogram(sch.linkage(X_scaled, method='ward'))
```

#### 聚类结果解读框架

```
地图相似度矩阵（示例）：
           Ancient  Dust2  Inferno  Mirage  Nuke  Anubis  Overpass
Ancient     1.00    0.85    0.45    0.40   0.30   0.50    0.35
Dust2       0.85    1.00    0.50    0.45   0.35   0.55    0.40
Inferno     0.45    0.50    1.00    0.60   0.50   0.70    0.55
...
```

**BP 含义**：相似度 > 0.7 的地图可以视为"可替代地图"——如果对手 ban 了你的首选图，你可以用相似图作为备选。

### 1.5 局限性

- 特征权重需要专家校准，不同赛季权重可能变化
- 仅基于静态属性，未考虑战队特定风格
- 聚类结果需结合人类分析师验证

---

## 2. BP 胜率预测模型

### 2.1 问题定义

**输入**：BP 过程产生的最终地图 + 对阵双方  
**输出**：Team A 在该地图上的胜率 P(win | map, team_A, team_B)

### 2.2 特征工程

#### 2.2.1 地图静态特征（M 类）

| 特征 | 类型 | 说明 |
|------|------|------|
| map_id | 类别 | 7 张 Active Duty 地图 |
| ct_win_rate_global | 数值 | 全局 CT 胜率 |
| t_win_rate_global | 数值 | 全局 T 胜率 |
| map_balance_score | 数值 | \|CT% - T%\| |
| map_cluster | 类别 | 聚类标签（CT堡垒/T快攻/均衡） |
| sniper_weight | 数值 | 狙击权重 1-10 |
| utility_weight | 数值 | 道具权重 1-10 |
| vertical_levels | 数值 | 垂直层级数 |

#### 2.2.2 战队动态画像（T 类）

| 特征 | 类型 | 窗口 | 说明 |
|------|------|------|------|
| team_map_win_rate | 数值 | 3/6/12 月 | 该队在该图上的胜率 |
| team_map_pick_rate | 数值 | 6 月 | 该队主动 pick 该图的频率 |
| team_map_ban_rate | 数值 | 6 月 | 该队主动 ban 该图的频率 |
| team_ct_rating | 数值 | 3 月 | 该队 CT 方平均 rating |
| team_t_rating | 数值 | 3 月 | 该队 T 方平均 rating |
| team_form_index | 数值 | 1 月 | 近期比赛胜率（指数衰减） |
| team_map_experience | 数值 | 12 月 | 该队在该图上的比赛场次 |
| team_map_variance | 数值 | 12 月 | 该队在该图上胜率的标准差（稳定性） |

#### 2.2.3 对阵历史特征（H 类）

| 特征 | 类型 | 说明 |
|------|------|------|
| h2h_map_win_rate | 数值 | 两队在该图上的历史交手胜率 |
| h2h_total_matches | 数值 | 两队在该图上的交手次数 |
| h2h_recent_trend | 数值 | 最近 3 次交手结果（指数衰减） |
| rank_diff | 数值 | HLTV 排名差 |
| map_ban_order | 数值 | 该图是在 BP 第几轮被 pick 的 |

#### 2.2.4 情境特征（C 类）

| 特征 | 类型 | 说明 |
|------|------|------|
| is_bo3 | 布尔 | 是否为 BO3 |
| is_bo5 | 布尔 | 是否为 BO5 |
| is_playoff | 布尔 | 是否为淘汰赛 |
| is_lan | 布尔 | 是否为线下赛 |
| map_pick_side | 类别 | pick 方先 CT 还是先 T |
| opponent_ban_preference | 数值 | 对手 ban 该图的倾向 |

### 2.3 模型选择

#### MVP 方案：逻辑回归 + 正则化

```python
from sklearn.linear_model import LogisticRegression
from sklearn.preprocessing import OneHotEncoder, StandardScaler
from sklearn.pipeline import Pipeline

model = Pipeline([
    ('scaler', StandardScaler()),
    ('classifier', LogisticRegression(
        penalty='elasticnet',
        solver='saga',
        C=1.0,
        l1_ratio=0.5,
        class_weight='balanced',
        max_iter=1000
    ))
])
```

**优点**：
- 可解释性强（系数即特征重要性）
- 训练快，适合在线更新
- 正则化可处理小样本问题

**缺点**：
- 无法自动捕捉特征交互
- 线性决策边界

#### 进阶方案 1：随机森林

```python
from sklearn.ensemble import RandomForestClassifier

model = RandomForestClassifier(
    n_estimators=500,
    max_depth=10,
    min_samples_leaf=5,
    class_weight='balanced_subsample',
    random_state=42
)
```

**优点**：
- 自动捕捉非线性关系和特征交互
- 特征重要性天然可解释
- 对缺失值和异常值鲁棒

**缺点**：
- 外推能力弱（对新战队/新地图组合预测差）
- 模型体积大

#### 进阶方案 2：贝叶斯分层模型（推荐用于小样本）

```python
import pymc as pm

with pm.Model() as hierarchical_model:
    # 全局截距
    mu_alpha = pm.Normal('mu_alpha', mu=0, sigma=1)
    sigma_alpha = pm.HalfNormal('sigma_alpha', sigma=1)
    
    # 地图效应
    mu_map = pm.Normal('mu_map', mu=0, sigma=1, shape=7)
    sigma_map = pm.HalfNormal('sigma_map', sigma=1, shape=7)
    map_effect = pm.Normal('map_effect', mu=mu_map, sigma=sigma_map, shape=7)
    
    # 战队效应（分层：全局→战队）
    mu_team = pm.Normal('mu_team', mu=0, sigma=1, shape=n_teams)
    sigma_team = pm.HalfNormal('sigma_team', sigma=1)
    team_effect = pm.Normal('team_effect', mu=mu_team, sigma=sigma_team, shape=n_teams)
    
    # 线性预测
    logits = mu_alpha + map_effect[map_idx] + team_effect[team_idx]
    
    # 似然
    obs = pm.Bernoulli('obs', logit_p=logits, observed=y)
```

**优点**：
- 天然处理小样本（通过分层收缩）
- 提供预测的不确定性区间
- 可融入先验知识

**缺点**：
- 训练时间长（MCMC 采样）
- 部署复杂

### 2.4 小样本问题处理策略

顶级战队（如 FaZe、NAVI、Vitality）之间的交手次数有限，这是本模型最大的挑战。

| 策略 | 方法 | 适用场景 |
|------|------|----------|
| **分层贝叶斯** | 战队效应从全局分布中采样，少量数据时收缩到全局均值 | 所有场景 |
| **迁移学习** | 先用大量中低级别比赛训练 base model，再用顶级比赛 fine-tune | MVP 后 |
| **数据增强** | 基于 ELO 系统生成合成对阵数据 | 冷启动 |
| **特征降维** | 用 PCA 压缩高维特征，减少参数数量 | 特征工程阶段 |
| **留一法交叉验证** | 每次留出一个顶级战队作为测试集 | 模型评估 |
| **Thompson Sampling** | 在预测时注入不确定性，避免过度自信 | 实时预测 |

**数据量建议**：

| 阶段 | 最低数据量 | 建议数据量 | 说明 |
|------|-----------|-----------|------|
| MVP 逻辑回归 | 500 场比赛 | 2000+ | 每图至少 70 场 |
| 随机森林 | 2000 场比赛 | 5000+ | 每图至少 300 场 |
| 贝叶斯分层 | 1000 场比赛 | 3000+ | 至少 20 支战队各 50 场 |
| 深度学习 | 10000+ | 50000+ | 需大量历史数据 |

### 2.5 模型评估

| 指标 | 目标值 | 说明 |
|------|--------|------|
| AUC-ROC | > 0.70 | 区分胜/负的能力 |
| Log Loss | < 0.60 | 概率校准质量 |
| Brier Score | < 0.22 | 预测准确度 |
| Calibration 曲线 | 接近对角线 | 概率校准 |
| Top-10 战队 MAE | < 8% | 顶级战队预测误差 |

### 2.6 局限性

- BP 只是比赛结果的一个因素（约 20-30%），无法完全预测
- 无法捕捉临场状态（选手当天手感、心理压力）
- 新地图（如 Anubis 2026 年初加入）数据稀疏
- 战队阵容变动（换人）后历史数据失效

---

## 3. 教练/分析师 BP 风格画像

### 3.1 目标

量化每个教练/IGL（In-Game Leader）的 BP 决策风格，识别模式、预测下一步操作。

### 3.2 BP 风格维度

#### 维度 1：激进 vs 保守

```
激进指数 = (主动 ban 热门图次数 + 主动 pick 冷门图次数) / 总 BP 次数
```

- **激进型**（激进指数 > 0.6）：主动 ban 对方强图，pick 自己的冷门图
  - 代表：karrigan (FaZe) — 喜欢出奇制胜
- **保守型**（激进指数 < 0.3）：ban 自己的弱图，pick 自己的强图
  - 代表：apEX (Vitality) — 坚持自己的舒适区
- **混合型**（0.3-0.6）：根据对手调整

#### 维度 2：预测性 vs 反应性

```
预测性指数 = (提前 ban 对手强图次数 - 等对手 ban 后再调整次数) / 总次数
```

- **预测型**：提前预判对手策略，先手 ban 关键图
- **反应型**：等对手出招后再调整

#### 维度 3：陷阱图倾向

```
陷阱指数 = (pick 自己近期输过的图次数 + pick 对手近期输过的图次数) / 总 pick 次数
```

- **高陷阱指数**：故意 pick 看似劣势的图，实则准备了特殊战术
- **低陷阱指数**：只 pick 自己最有信心的图

#### 维度 4：BP 序列模式

将 BP 过程建模为序列：

```
Ban_A1 → Ban_B1 → Ban_A2 → Ban_B2 → Pick_A → Pick_B → Ban_A3 → Ban_B3 → Pick_B → Pick_A
```

使用 **序列模式挖掘**（PrefixSpan / SPADE）提取常见模式。

### 3.3 画像方法

#### 3.3.1 基于统计的画像（MVP）

```python
# 每个教练的 BP 风格向量
coach_profile = {
    'aggression_score': 0.72,       # 激进指数
    'predictive_score': 0.45,       # 预测性指数
    'trap_score': 0.38,             # 陷阱指数
    'preferred_first_ban': 'Nuke',  # 最常首 ban 的地图
    'preferred_pick': 'Mirage',     # 最常 pick 的地图
    'ban_diversity': 0.65,          # ban 图多样性（Shannon 熵）
    'pick_diversity': 0.55,         # pick 图多样性
    'side_preference': 'CT',        # 优先选哪一方
}
```

#### 3.3.2 基于聚类的教练分类

使用 K-means 对教练画像进行聚类：

```
Cluster A: "激进赌徒" — 高激进 + 高陷阱 + 高多样性
  → 代表：karrigan, HooXi
  → 对策：准备反陷阱策略，不要被带节奏

Cluster B: "保守专家" — 低激进 + 低陷阱 + 低多样性
  → 代表：apEX, Snappi
  → 对策：在 BP 中迫使对方离开舒适区

Cluster C: "数据分析师" — 中激进 + 高预测性 + 中陷阱
  → 代表：B1ad3, zonic
  → 对策：准备数据对抗，BP 中制造不确定性

Cluster D: "随机游走" — 高多样性 + 低预测性
  → 代表：新教练/阵容不稳定的队伍
  → 对策：无法预测，需要实时适应
```

### 3.4 心理博弈量化

#### 3.4.1 "反向 BP" 检测

当教练的 BP 行为与历史模式显著偏离时，标记为"反向 BP"：

```python
def detect_reverse_bp(current_bp, historical_pattern):
    deviation = cosine_distance(current_bp_vector, historical_pattern)
    return deviation > threshold  # 阈值通过训练数据确定
```

#### 3.4.2 "陷阱图" 识别

```python
def identify_trap_map(map_picked, picker_team, opponent_team):
    """
    判断某次 pick 是否为陷阱图
    """
    signals = []
    
    # 信号 1：该队近期在这张图上表现不佳
    if picker_team.recent_win_rate(map_picked) < 0.4:
        signals.append('recent_poor_form')
    
    # 信号 2：对手在这张图上表现很好
    if opponent_team.win_rate(map_picked) > 0.6:
        signals.append('opponent_strong')
    
    # 信号 3：该队从未在正式比赛中 pick 过这张图
    if picker_team.pick_count(map_picked) < 2:
        signals.append('unusual_pick')
    
    # 信号 4：该队最近在训练赛中大量练习这张图（需训练赛数据）
    if picker_team.practice_rate(map_picked) > 0.3:
        signals.append('prepared_trap')
    
    return {
        'is_trap': len(signals) >= 2,
        'confidence': len(signals) / 4,
        'signals': signals
    }
```

### 3.5 局限性

- 教练画像需要至少 20-30 场 BP 数据才能稳定
- 教练换队后历史画像需要重置或衰减
- 无法区分教练 vs IGL 在 BP 中的实际决策权重
- 陷阱图识别依赖训练赛数据（通常不公开）

---

## 4. 版本更新影响分析

### 4.1 问题

地图版本更新后，历史数据在多大程度上仍然有效？如何衰减？

### 4.2 数据衰减策略

#### 4.2.1 指数衰减权重

```python
def match_weight(match_date, patch_date, half_life_days=90):
    """
    计算比赛权重
    - patch_date: 地图版本更新日期
    - half_life_days: 半衰期（默认 90 天）
    """
    days_since_patch = (match_date - patch_date).days
    if days_since_patch < 0:
        # 版本更新前的比赛，权重降低
        return 0.3 * (0.5 ** (abs(days_since_patch) / half_life_days))
    else:
        # 版本更新后的比赛，权重正常
        return 0.5 ** (days_since_patch / (half_life_days * 2))
```

#### 4.2.2 分阶段衰减

```
版本更新前 180 天以上数据：权重 0.1（几乎不用）
版本更新前 90-180 天数据：权重 0.3
版本更新前 0-90 天数据：权重 0.5
版本更新后 0-30 天数据：权重 0.8（冷启动期）
版本更新后 30-90 天数据：权重 1.0
版本更新后 90 天以上数据：权重 0.8（逐渐过时）
```

#### 4.2.3 地图版本哈希

为每个地图版本生成唯一哈希：

```
Ancient_v2025.08 = hash("Ancient", "2025-08-patch")
Ancient_v2026.01 = hash("Ancient", "2026-01-patch")
```

模型训练时只使用同一版本或相邻版本的数据。

### 4.3 BP 格局变化检测

#### 4.3.1 统计检测方法

```python
from scipy import stats
import numpy as np

def detect_meta_shift(before_data, after_data, metric='ban_rate'):
    """
    检测版本更新前后 BP 格局是否显著变化
    """
    before = before_data[metric]
    after = after_data[metric]
    
    # 方法 1：双样本 t 检验
    t_stat, p_value = stats.ttest_ind(before, after)
    
    # 方法 2：Cohen's d 效应量
    n1, n2 = len(before), len(after)
    s1, s2 = np.var(before, ddof=1), np.var(after, ddof=1)
    s_pooled = np.sqrt(((n1-1)*s1 + (n2-1)*s2) / (n1+n2-2))
    cohens_d = (np.mean(before) - np.mean(after)) / s_pooled
    
    return {
        'p_value': p_value,
        'significant': p_value < 0.05,
        'effect_size': abs(cohens_d),
        'meta_shift': abs(cohens_d) > 0.5  # 中等以上效应量视为格局变化
    }
```

#### 4.3.2 变化点检测（Change Point Detection）

```python
import ruptures as rpt

def find_meta_change_points(time_series_data, metric='ban_rate'):
    """
    使用 PELT 算法检测 BP 格局变化点
    """
    signal = time_series_data[metric].values.reshape(-1, 1)
    
    # PELT 算法
    model = rpt.Pelt(model="rbf").fit(signal)
    change_points = model.predict(pen=10)
    
    return change_points
```

#### 4.3.3 可视化指标

| 指标 | 说明 | 更新后预期变化 |
|------|------|---------------|
| 地图 ban 率变化 | 每张图被 ban 的频率变化 | 新版本地图可能被 ban 更多 |
| 地图 pick 率变化 | 每张图被 pick 的频率变化 | 战术变化导致 pick 率转移 |
| 地图胜率变化 | CT/T 胜率变化 | 地图平衡性调整的直接反映 |
| 战队地图池变化 | 各战队 pick/ban 行为变化 | 战队适应新版本的速度 |
| BP 序列模式变化 | 常见 BP 序列的变化 | 新的 BP 套路出现 |

### 4.4 版本更新应对策略

```
版本更新 → 检测变化幅度

  ├─ 小幅更新（p > 0.05, d < 0.3）
  │   └─ 正常衰减，无需特殊处理
  │
  ├─ 中幅更新（p < 0.05, 0.3 < d < 0.8）
  │   ├─ 降低旧数据权重至 0.3
  │   ├─ 标记受影响的地图
  │   └─ 显示"版本更新后数据有限"警告
  │
  └─ 大幅更新（p < 0.01, d > 0.8）
      ├─ 旧数据权重降至 0.1
      ├─ 启用冷启动模式（更多依赖地图静态特征）
      ├─ 增加"不确定性"标记
      └─ 建议用户手动分析
```

### 4.5 局限性

- 版本更新后需要至少 2-4 周的新数据才能重新校准
- 无法区分"版本变化"和"战队状态变化"的影响
- 大型赛事（Major）期间的数据权重需要特殊处理

---

## 5. 数据管道与工程架构

### 5.1 数据流

```
HLTV API / 爬虫
    │
    ▼
原始数据层 (Raw Data)
    ├─ match_results (比赛结果)
    ├─ map_stats (地图统计)
    ├─ team_profiles (战队画像)
    ├─ player_stats (选手数据)
    └─ bp_sequences (BP 序列)
    │
    ▼
特征工程层 (Feature Engineering)
    ├─ map_features.py → 地图特征向量
    ├─ team_features.py → 战队动态画像
    ├─ h2h_features.py → 对阵历史特征
    └─ context_features.py → 情境特征
    │
    ▼
模型层 (Model Layer)
    ├─ clustering/ → 地图聚类
    ├─ prediction/ → 胜率预测
    └─ profiling/ → 教练画像
    │
    ▼
API 层 (API Layer)
    ├─ /api/v1/predict  → BP 胜率预测
    ├─ /api/v1/cluster  → 地图聚类结果
    ├─ /api/v1/profile  → 教练画像
    └─ /api/v1/meta     → 版本更新分析
    │
    ▼
前端展示 (Frontend)
    ├─ 地图聚类可视化
    ├─ BP 胜率预测面板
    ├─ 教练画像卡片
    └─ 版本更新时间线
```

### 5.2 数据源

| 数据源 | 类型 | 获取方式 | 更新频率 |
|--------|------|----------|----------|
| HLTV.org | 比赛数据 | API / 爬虫 | 每日 |
| Leetify | 地图统计 | 公开数据 | 每月 |
| Liquipedia | 赛事信息 | API | 实时 |
| Twitch/YouTube | 训练赛数据 | 文本分析 | 可选 |

### 5.3 技术栈建议

| 组件 | MVP 方案 | 进阶方案 |
|------|----------|----------|
| 数据处理 | Pandas + NumPy | Spark / Dask |
| 特征存储 | CSV / SQLite | PostgreSQL + Redis |
| 模型训练 | scikit-learn | PyMC / PyTorch |
| 模型部署 | Flask API | FastAPI + Docker |
| 模型更新 | 手动重训练 | 自动 CI/CD 管道 |
| 前端展示 | ECharts / D3.js | React + D3.js |
| 缓存 | 无 | Redis (预测结果缓存) |

### 5.4 更新频率

| 模型/数据 | 更新频率 | 触发条件 |
|-----------|----------|----------|
| 地图静态特征 | 版本更新时 | Valve 发布新补丁 |
| 战队动态画像 | 每日 | 新比赛数据 |
| 胜率预测模型 | 每周 | 累积 50+ 新比赛 |
| 教练画像 | 每场比赛后 | 新 BP 数据 |
| 版本更新检测 | 版本更新后 2 周 | 新补丁 + 足够数据 |

---

## 6. MVP 路线图与迭代路径

### Phase 1: MVP（4-6 周）

**目标**：可用的基础分析工具

- [ ] 地图聚类（K-means, k=3）
- [ ] BP 胜率预测（逻辑回归）
- [ ] 教练画像（统计型，4 维度）
- [ ] 基础数据管道（HLTV 爬虫 + SQLite）
- [ ] 前端展示（地图聚类图 + 预测面板）

**数据量要求**：500+ 场比赛

### Phase 2: 增强（2-3 个月）

**目标**：提高准确性和深度

- [ ] 层次聚类 + PCA 可视化
- [ ] 随机森林预测模型
- [ ] 教练聚类分类
- [ ] 版本更新检测（t 检验）
- [ ] 指数衰减权重
- [ ] 陷阱图识别

**数据量要求**：2000+ 场比赛

### Phase 3: 高级（3-6 个月）

**目标**：专业级分析工具

- [ ] 贝叶斯分层模型
- [ ] 序列模式挖掘（BP 序列）
- [ ] 变化点检测（PELT 算法）
- [ ] 反向 BP 检测
- [ ] 自动版本更新报告
- [ ] 训练赛数据整合（如可用）

**数据量要求**：5000+ 场比赛

### Phase 4: 生产化（持续）

**目标**：可靠的生产系统

- [ ] 自动数据管道
- [ ] 模型监控和漂移检测
- [ ] A/B 测试框架
- [ ] 用户反馈回路
- [ ] 多语言支持
- [ ] 移动端适配

---

## 7. 附录：特征工程参考表

### 7.1 完整特征清单

```
特征类别: M = 地图静态, T = 战队动态, H = 对阵历史, C = 情境
特征类型: N = 数值, C = 类别, B = 布尔

ID       类别  类型  特征名                   说明
─────────────────────────────────────────────────────
M01      M     C     map_id                   地图 ID
M02      M     N     ct_win_rate_global       全局 CT 胜率
M03      M     N     t_win_rate_global        全局 T 胜率
M04      M     N     map_balance              |CT% - T%|
M05      M     N     sniper_weight            狙击权重 1-10
M06      M     N     utility_weight           道具权重 1-10
M07      M     N     vertical_levels          垂直层级数
M08      M     N     key_control_zones        关键控制区数量
M09      M     N     rotate_time_avg          平均回防时间(秒)
M10      M     N     avg_round_length         平均回合时长(秒)
M11      M     C     map_cluster              聚类标签
─────────────────────────────────────────────────────
T01      T     N     team_map_win_rate_3m     近 3 月该图胜率
T02      T     N     team_map_win_rate_6m     近 6 月该图胜率
T03      T     N     team_map_win_rate_12m    近 12 月该图胜率
T04      T     N     team_map_pick_rate       该图 pick 频率
T05      T     N     team_map_ban_rate        该图 ban 频率
T06      T     N     team_ct_rating_3m        CT 方 rating
T07      T     N     team_t_rating_3m         T 方 rating
T08      T     N     team_form_index          近期状态指数
T09      T     N     team_map_experience      该图比赛场次
T10      T     N     team_map_variance        该图胜率标准差
T11      T     N     team_rank                HLTV 排名
─────────────────────────────────────────────────────
H01      H     N     h2h_map_win_rate         对阵该图胜率
H02      H     N     h2h_total_matches        对阵该图场次
H03      H     N     h2h_recent_trend         近期交手趋势
H04      H     N     rank_diff                排名差
H05      H     N     map_ban_order            BP 轮次
─────────────────────────────────────────────────────
C01      C     B     is_bo3                   是否 BO3
C02      C     B     is_bo5                   是否 BO5
C03      C     B     is_playoff               是否淘汰赛
C04      C     B     is_lan                   是否线下赛
C05      C     C     map_pick_side            pick 方起始阵营
C06      C     N     opponent_ban_preference  对手 ban 倾向
```

### 7.2 特征重要性排序（预期）

基于 CS 职业比赛领域知识，特征重要性预期排序：

1. **team_map_win_rate**（战队-地图胜率）— 最强预测因子
2. **h2h_map_win_rate**（对阵历史）— 特定对阵的关键
3. **team_form_index**（近期状态）— 时效性重要
4. **map_balance**（地图均衡度）— 地图本身的影响
5. **team_ct/t_rating**（阵营能力）— 阵营优势
6. **rank_diff**（排名差）— 实力差距
7. **is_playoff**（淘汰赛）— 心理压力因素
8. **map_ban_order**（BP 轮次）— BP 策略深度

---

## 参考资料

1. **Bandit Modeling of Map Selection in CS:GO** — Mer斯坦 et al. (2023)  
   Contextual bandit 框架分析 CS:GO BP 决策，25,000+ BP 决策数据

2. **Predicting Game Outcome of Counter Strike with Machine Learning** — Fordham University (2023)  
   使用 HLTV 数据预测 CS:GO 比赛结果，准确率 68-73%

3. **A Generative AI Approach Using BERT for Ban/Pick Prediction in DotA 2** — Liu et al. (2024)  
   BERT 模型预测 DotA 2 BP 行为，2,295 场比赛

4. **HLTV.org** — 职业 CS 比赛统计数据源  
   https://www.hltv.org/stats/maps

5. **Leetify Data Library** — CS2 地图胜率统计  
   https://leetify.com/data-library/counter-strike/

6. **Skinrave / BitSkins** — CS2 地图 CT/T 胜率分析  
   https://bitskins.com/blog/cs2-map-sides-which-maps-are-t-or-ct-sided

---

> **文档维护**：本设计文档将随项目进展持续更新。  
> **下一阶段**：前端原型设计与数据管道实现。
