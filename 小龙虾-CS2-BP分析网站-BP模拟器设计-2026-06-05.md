# 🦞 小龙虾-CS2-BP分析网站-BP模拟器设计

> **版本:** v1.0  
> **日期:** 2026-06-05  
> **作者:** 小龙虾团队  
> **状态:** 设计文档  
> **关联:** 第一层 — 数据看板（另行设计）

---

## 目录

1. [概述](#1-概述)
2. [BP 规则引擎](#2-bp-规则引擎)
3. [状态机设计](#3-状态机设计)
4. [UI 交互设计](#4-ui-交互设计)
5. [推荐策略引擎](#5-推荐策略引擎)
6. [历史回放与 What-if 分析](#6-历史回放与-what-if-分析)
7. [技术选型](#7-技术选型)
8. [与第一层数据看板的接口对接](#8-与第一层数据看板的接口对接)
9. [多赛制支持](#9-多赛制支持)
10. [数据模型](#10-数据模型)
11. [API 设计](#11-api-设计)
12. [开发路线图](#12-开发路线图)

---

## 1. 概述

### 1.1 目标

构建一个**交互式 BP 模拟器**，让用户能够：

- 选择两支队伍 → 模拟完整的 BP 流程
- 每一步看到**推荐策略**及**数据支撑**
- 回放历史对阵的 BP 过程
- 执行 **"What-if" 假设分析**：如果换一种 Ban 法会怎样

### 1.2 当前 Active Duty 地图池（2026年）

| 地图 | 缩写 | 类型 | 特点 |
|------|------|------|------|
| Ancient | AN | 古遗迹 | 中路控制权争夺激烈 |
| Dust2 | D2 | 沙漠 | 经典平衡图，枪法说话 |
| Inferno | INF | 炼狱小镇 | 香蕉道控制核心 |
| Mirage | MIR | 荒漠迷城 | 最均衡竞技图 |
| Nuke | NUK | 核子危机 | 垂直立体作战 |
| Anubis | ANU | 阿努比斯 | 新图，A 点复杂 |
| Overpass | OVP | 死亡游乐园 | 工地图，长枪图 |

### 1.3 支持的赛制

| 赛制 | 地图数 | 适用场景 |
|------|--------|----------|
| BO1 | 1 图定胜负 | 小组赛、瑞士轮 |
| BO3 | 3 局 2 胜 | 标准淘汰赛 |
| BO5 | 5 局 3 胜 | 决赛 |

---

## 2. BP 规则引擎

### 2.1 Valve 官方规则（Major 标准）

#### BO1 流程
```
1. 高种子队选择为 Team A 或 Team B
2. Team A 移除 2 张地图
3. Team B 移除 3 张地图
4. Team A 移除 1 张地图
5. Team B 选择起始阵营（CT/T）
→ 剩 1 张地图为比赛图
```

#### BO3 流程（蛇形 Ban/Pick）
```
1. 高种子队选择为 Team A 或 Team B
2. Team A 禁 1 张
3. Team B 禁 1 张
4. Team A 选 第1图  → Team B 选 第1图阵营
5. Team B 选 第2图  → Team A 选 第2图阵营
6. Team B 禁 1 张
7. Team A 禁 1 张
8. Team B 选 第3图阵营
→ 剩余 1 张为第3图（决胜图）
```

#### BO5 流程
```
1. 高种子队选择为 Team A 或 Team B
2. Team A 禁 1 张
3. Team B 禁 1 张
4. Team A 选 第1图  → Team B 选 第1图阵营
5. Team B 选 第2图  → Team A 选 第2图阵营
6. Team A 选 第3图  → Team B 选 第3图阵营
7. Team B 选 第4图  → Team A 选 第4图阵营
8. 剩余 1 张为第5图 → Team B 选 第5图阵营
```

### 2.2 传统社区规则（ESEA/FACEIT 风格）

部分赛事使用不同的 Ban/Pick 顺序，引擎需要支持可配置规则：

#### BO3 传统风格
```
1. Team A 禁 1
2. Team B 禁 1
3. Team A 选 1 (图1) → Team B 选阵营
4. Team B 选 1 (图2) → Team A 选阵营
5. Team A 禁 1
6. Team B 禁 1
7. 剩图决胜 (图3) → Team B 选阵营
```

> 注意：传统风格与 Valve 官方规则在**第 5-6 步的禁图顺序**上可能不同。引擎需支持两种模式。

---

## 3. 状态机设计

### 3.1 BP 状态机（核心）

```
┌─────────────────────────────────────────────────────────────────┐
│                        BP 状态机                                  │
│                                                                   │
│  ┌──────────┐     ┌──────────┐     ┌──────────────┐              │
│  │  INIT    │────>│ SIDE_SEL │────>│  BAN_PHASE   │              │
│  │ (初始化) │     │ (选边)   │     │  (禁图阶段)  │              │
│  └──────────┘     └──────────┘     └──────┬───────┘              │
│                                            │                      │
│                                            ▼                      │
│                                   ┌──────────────┐              │
│                                   │  PICK_PHASE  │              │
│                                   │  (选图阶段)  │              │
│                                   └──────┬───────┘              │
│                                            │                      │
│                                            ▼                      │
│                                   ┌──────────────┐              │
│                                   │ SIDE_PICK    │              │
│                                   │ (选阵营阶段) │              │
│                                   └──────┬───────┘              │
│                                            │                      │
│                                            ▼                      │
│                                   ┌──────────────┐              │
│                                   │  DECIDER     │              │
│                                   │ (决胜图阶段) │              │
│                                   └──────┬───────┘              │
│                                            │                      │
│                                            ▼                      │
│                                   ┌──────────────┐              │
│                                   │  COMPLETED   │              │
│                                   │  (完成)      │              │
│                                   └──────────────┘              │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 状态定义

```typescript
enum BPState {
  // 初始状态
  INIT = 'INIT',
  
  // 赛制选择
  FORMAT_SELECT = 'FORMAT_SELECT',
  
  // 队伍选择
  TEAM_SELECT = 'TEAM_SELECT',
  
  // 选边（高种子队选择为 Team A 或 Team B）
  SIDE_SELECT = 'SIDE_SELECT',
  
  // 禁图阶段
  BAN_PHASE = 'BAN_PHASE',
  
  // 选图阶段
  PICK_PHASE = 'PICK_PHASE',
  
  // 选阵营阶段
  SIDE_PICK = 'SIDE_PICK',
  
  // 决胜图（自动分配剩余图）
  DECIDER = 'DECIDER',
  
  // 完成
  COMPLETED = 'COMPLETED',
  
  // 错误/回退
  ERROR = 'ERROR'
}
```

### 3.3 状态转换表

| 当前状态 | 触发事件 | 下一状态 | 动作 |
|----------|----------|----------|------|
| INIT | `SELECT_FORMAT` | FORMAT_SELECT | 设置赛制(BO1/BO3/BO5) |
| FORMAT_SELECT | `SELECT_TEAMS` | TEAM_SELECT | 选择两支队伍 |
| TEAM_SELECT | `CONFIRM_TEAMS` | SIDE_SELECT | 确认队伍，高种子自动或手动选边 |
| SIDE_SELECT | `SET_SIDES` | BAN_PHASE | 确定 Team A/B |
| BAN_PHASE | `BAN_MAP` | BAN_PHASE / PICK_PHASE | 禁图，检查是否进入选图阶段 |
| PICK_PHASE | `PICK_MAP` | SIDE_PICK / BAN_PHASE | 选图，对方选阵营 |
| SIDE_PICK | `PICK_SIDE` | PICK_PHASE / BAN_PHASE / DECIDER | 选阵营，检查流程下一步 |
| DECIDER | `AUTO_ASSIGN` | COMPLETED | 剩余图自动分配为决胜图 |
| COMPLETED | `RESET` | INIT | 重置流程 |
| * | `UNDO` | 上一状态 | 回退一步 |
| * | `ERROR` | ERROR | 错误处理 |

### 3.4 BO3 完整状态转换示例

```
INIT → FORMAT_SELECT(BO3) → TEAM_SELECT(Navi, FaZe) → SIDE_SELECT(Navi=TeamA)
→ BAN_PHASE(Navi禁Ancient) → BAN_PHASE(FaZe禁Dust2)
→ PICK_PHASE(Navi选Mirage) → SIDE_PICK(FaZe选CT)
→ PICK_PHASE(FaZe选Inferno) → SIDE_PICK(Navi选CT)
→ BAN_PHASE(FaZe禁Nuke) → BAN_PHASE(Navi禁Anubis)
→ DECIDER(Overpass自动分配) → SIDE_PICK(FaZe选CT)
→ COMPLETED
```

### 3.5 状态机实现要点

```typescript
// 核心状态机接口
interface BPStateMachine {
  currentState: BPState;
  history: BPStateHistory[];
  
  // 核心方法
  transition(event: BPEvent, payload?: any): BPResult;
  undo(): BPResult;
  getAvailableActions(): BPAction[];
  getSnapshot(): BPSnapshot;
  
  // 验证
  validateAction(action: BPAction): boolean;
}

// 每一步的历史记录
interface BPStateHistory {
  step: number;
  state: BPState;
  action: BPAction;
  actor: 'team_a' | 'team_b';
  map?: MapInfo;
  side?: 'CT' | 'T';
  timestamp: number;
  recommendation?: StrategyRecommendation;
}
```

---

## 4. UI 交互设计

### 4.1 主界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  [Logo]  CS2 BP 模拟器    [赛制: BO3 ▼]  [规则: Valve ▼]   │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────────────┐  ┌─────────────┐   │
│  │  Team A      │  │   地图池区域      │  │  Team B      │   │
│  │  [Navi]      │  │                  │  │  [FaZe]      │   │
│  │  种子: #1    │  │  ┌──┐ ┌──┐ ┌──┐ │  │  种子: #3    │   │
│  │              │  │  │AN│ │D2│ │IN│ │  │              │   │
│  │  胜率: 68%   │  │  └──┘ └──┘ └──┘ │  │  胜率: 65%   │   │
│  │              │  │  ┌──┐ ┌──┐ ┌──┐ │  │              │   │
│  │  [历史BP]    │  │  │MI│ │NU│ │AN│ │  │  [历史BP]    │   │
│  │              │  │  └──┘ └──┘ └──┘ │  │              │   │
│  └─────────────┘  │  ┌──┐            │  └─────────────┘   │
│                    │  │OV│            │                    │
│                    │  └──┘            │                    │
│                    └──────────────────┘                    │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────┐   │
│  │  BP 流程时间线                                       │   │
│  │                                                     │   │
│  │  Step 1: Navi 禁 Ancient  [推荐: 建议禁Dust2]       │   │
│  │  Step 2: FaZe 禁 Dust2   [推荐: 建议禁Ancient]      │   │
│  │  Step 3: Navi 选 Mirage  [推荐: 建议选Mirage]       │   │
│  │  Step 4: FaZe 选 Inferno [推荐: 建议选Inferno]      │   │
│  │  Step 5: FaZe 禁 Nuke    [推荐: 建议禁Anubis]       │   │
│  │  Step 6: Navi 禁 Anubis  [推荐: 建议禁Overpass]     │   │
│  │  Step 7: 决胜图 Overpass [FaZe 选 CT]               │   │
│  │                                                     │   │
│  │  [← 上一步]  [重置]  [保存BP]  [分享]               │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌──────────────────────────┐    │
│  │  当前步骤推荐         │  │  数据面板                │    │
│  │                      │  │                          │    │
│  │  📊 推荐策略          │  │  Navi on Mirage:        │    │
│  │  Navi 应选 Mirage    │  │  近3月胜率 72%          │    │
│  │                      │  │  对阵FaZe胜率 68%       │    │
│  │  理由:               │  │  CT方胜率 65%           │    │
│  │  - Navi Mirage 胜率  │  │  T方胜率 55%            │    │
│  │    72% (高)          │  │                          │    │
│  │  - FaZe Mirage 胜率  │  │  [查看详细数据 →]       │    │
│  │    48% (低)          │  │                          │    │
│  │  - 地图差 +24%       │  │                          │    │
│  │                      │  │                          │    │
│  └──────────────────────┘  └──────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 交互流程

#### 4.2.1 地图卡片交互

每张地图卡片支持三种状态，通过视觉样式区分：

```
┌──────────────────┐
│  可用 (Available) │  → 正常显示，可点击/拖拽
│  ┌────────────┐  │
│  │  Mirage    │  │
│  │  胜率 72%  │  │
│  └────────────┘  │
├──────────────────┤
│  已禁 (Banned)   │  → 灰色/半透明，带"禁"标记
│  ┌────────────┐  │
│  │  ░Ancient░ │  │
│  │  ░ 禁  ░   │  │
│  └────────────┘  │
├──────────────────┤
│  已选 (Picked)   │  → 高亮边框，带"选"标记 + 阵营
│  ┌────────────┐  │
│  │  ▓Mirage ▓ │  │
│  │  ▓ CT方 ▓  │  │
│  └────────────┘  │
└──────────────────┘
```

#### 4.2.2 拖拽交互

- **拖拽地图到 Team A/B 区域** = 该队禁图
- **拖拽地图到"已选"区域** = 该队选图
- **点击地图** = 弹出操作菜单（禁/选/查看数据）
- **悬停地图** = 显示该图两队详细数据对比

#### 4.2.3 点击交互（备选方案）

- 当前轮到哪队，该队区域高亮闪烁
- 点击可用地图 → 弹出确认对话框
  - 如果是禁图阶段 → "确认禁掉 [地图名]?"
  - 如果是选图阶段 → "确认选择 [地图名]?"
- 确认后进入下一步

#### 4.2.4 推荐气泡

每一步操作前，显示推荐气泡：

```
┌─────────────────────────────────────┐
│  💡 推荐策略                        │
│                                     │
│  Navi 应选 Mirage                   │
│  ┌─────────────────────────────┐    │
│  │ 理由:                        │    │
│  │  • Navi 在 Mirage 胜率 72%   │    │
│  │  • FaZe 在 Mirage 胜率 48%   │    │
│  │  • 地图差 +24% (最大优势图)  │    │
│  │  • 近5次交手 Navi 4-1        │    │
│  └─────────────────────────────┘    │
│                                     │
│  [采纳推荐]  [查看其他选项]         │
└─────────────────────────────────────┘
```

### 4.3 BP 流程时间线组件

```
Step 1 ──●───────○───────○───────○───────○───────○───────○
         Navi禁  FaZe禁  Navi选  FaZe选  FaZe禁  Navi禁  决胜图
         Ancient Dust2  Mirage Inferno Nuke   Anubis  Overpass
         
         [已完成] [已完成] [当前]  [待办]  [待办]  [待办]  [待办]
```

### 4.4 响应式布局

| 断点 | 布局 |
|------|------|
| ≥1200px | 三栏：队伍A | 地图池 | 队伍B |
| 768-1199px | 两栏：地图池在上，队伍信息在下 |
| <768px | 单栏垂直布局，地图池可横向滚动 |

---

## 5. 推荐策略引擎

### 5.1 推荐算法

#### 5.1.1 基础推荐（基于胜率）

```typescript
interface StrategyRecommendation {
  type: 'ban' | 'pick' | 'side';
  targetMap?: string;
  targetSide?: 'CT' | 'T';
  confidence: number; // 0-100
  reasons: string[];
  alternatives: AlternativeOption[];
  dataPoints: DataPoint[];
}

interface DataPoint {
  label: string;
  teamA: number;
  teamB: number;
  diff: number;
}

// 推荐算法
function getRecommendation(
  state: BPState,
  teamA: TeamStats,
  teamB: TeamStats,
  availableMaps: MapInfo[]
): StrategyRecommendation {
  switch (state) {
    case 'BAN_PHASE':
      // 禁图推荐：禁掉对方最强图
      return recommendBan(teamA, teamB, availableMaps);
    case 'PICK_PHASE':
      // 选图推荐：选自己最强图
      return recommendPick(teamA, teamB, availableMaps);
    case 'SIDE_PICK':
      // 阵营推荐：选胜率更高的阵营
      return recommendSide(teamA, teamB, currentMap);
  }
}
```

#### 5.1.2 高级推荐（多因子）

```
推荐分数 = w1 × 胜率差 + w2 × 近期状态 + w3 × 交手记录 + w4 × 地图池深度

其中:
- 胜率差: 两队在该图胜率之差 (权重 0.4)
- 近期状态: 近10场该图表现 (权重 0.25)
- 交手记录: 两队在该图历史交手 (权重 0.2)
- 地图池深度: 剩余可用地图的强度分布 (权重 0.15)
```

#### 5.1.3 策略类型

| 策略类型 | 描述 | 适用场景 |
|----------|------|----------|
| 进攻型 | 选自己最强图，禁对方最强图 | 实力占优 |
| 防守型 | 禁自己最弱图，留对方中等图 | 实力劣势 |
| 针对型 | 针对对方特定选手/战术 | 有明确弱点 |
| 随机型 | 不可预测的 Ban/Pick | 迷惑对手 |

### 5.2 数据来源

推荐引擎依赖第一层数据看板提供的数据：

```typescript
interface TeamStats {
  teamId: string;
  teamName: string;
  globalWinRate: number;
  mapStats: Map<MapId, MapStats>;
  recentForm: RecentForm;
  headToHead: HeadToHead[];
}

interface MapStats {
  mapId: MapId;
  winRate: number;        // 总胜率
  ctWinRate: number;      // CT方胜率
  tWinRate: number;       // T方胜率
  matchesPlayed: number;  // 场次
  last10Wins: number;     // 近10场胜场
  trend: 'up' | 'down' | 'stable'; // 趋势
}
```

---

## 6. 历史回放与 What-if 分析

### 6.1 历史 BP 回放

#### 6.1.1 数据存储

```typescript
interface BPSession {
  id: string;
  matchId?: string;        // 关联的比赛ID
  format: 'BO1' | 'BO3' | 'BO5';
  ruleSet: 'valve' | 'traditional';
  teamA: TeamInfo;
  teamB: TeamInfo;
  steps: BPStateHistory[];
  result: BPResult;
  createdAt: string;
  tags: string[];          // 标签，如 "major-2026", "final"
}
```

#### 6.1.2 回放功能

- **步骤回放**：逐帧播放 BP 过程
- **速度控制**：1x / 2x / 4x / 手动
- **时间跳转**：点击时间线任意节点跳转
- **注释模式**：每步显示当时的数据分析和推荐

#### 6.1.3 回放 UI

```
┌────────────────────────────────────────────────────────────┐
│  历史 BP 回放: Navi vs FaZe | Major 2026 决赛              │
├────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────┐    │
│  │  ▶  ⏸  ⏹  [1x ▼]  [◀◀] [▶▶]  进度: 3/7        │    │
│  │  ●━━━━━━━━━●━━━━━●━━━━━━━━●━━━━━●━━━━━●━━━━━●    │    │
│  │  禁Ancient  禁D2  选Mirage 选INF 禁Nuke 禁Anu  决OVP│    │
│  └────────────────────────────────────────────────────┘    │
│                                                            │
│  当前步骤: Step 3 - Navi 选 Mirage                        │
│  ┌──────────────┐  ┌──────────────────────────────────┐   │
│  │  当时数据      │  │  当时推荐                        │   │
│  │  Navi Mirage  │  │  ✅ 推荐选 Mirage (采纳)         │   │
│  │  胜率: 71%    │  │  备选: Inferno (65%)             │   │
│  │  FaZe Mirage  │  │  不推荐: Nuke (42%)              │   │
│  │  胜率: 45%    │  │                                  │   │
│  └──────────────┘  └──────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

### 6.2 What-if 假设分析

#### 6.2.1 核心概念

用户可以在 BP 流程的任意步骤**分支**，选择不同的操作，系统会：

1. 基于当前状态重新计算后续推荐
2. 对比两条分支的最终结果
3. 显示"如果...会怎样"的数据差异

#### 6.2.2 分支管理

```
时间线:
┌─ 实际BP ──────────────────────────────────────────────┐
│  Navi禁A → FaZe禁B → Navi选M → FaZe选I → FaZe禁N → ...│
└────────────────────────────────────────────────────────┘
                           │
                           │ What-if 分支点
                           ▼
┌─ What-if ─────────────────────────────────────────────┐
│  Navi禁A → FaZe禁B → Navi选M → FaZe选O(不同) → ...    │
└────────────────────────────────────────────────────────┘
```

#### 6.2.3 对比视图

```
┌─────────────────────────────────────────────────────────┐
│  What-if 对比: Step 4 FaZe 选图                          │
├─────────────────────────────────────────────────────────┤
│                   实际选择         What-if 选择           │
│  选图:           Inferno         Overpass               │
│  FaZe胜率:       58%             62%                    │
│  Navi胜率:       62%             55%                    │
│  地图差:         -4%             +7%                    │
│                                                        │
│  后续影响:                                             │
│  决胜图:         Overpass        Nuke                   │
│  FaZe决胜胜率:   55%             48%                    │
│                                                        │
│  综合预测胜率:                                          │
│  FaZe:           52%             55% (+3%)              │
│  Navi:           48%             45% (-3%)              │
└─────────────────────────────────────────────────────────┘
```

#### 6.2.4 What-if 实现逻辑

```typescript
class WhatIfAnalyzer {
  private originalPath: BPSession;
  private branches: Map<string, BPSession> = new Map();
  
  // 创建分支
  createBranch(
    branchPoint: number,      // 在哪个步骤分支
    alternativeAction: BPAction  // 替代操作
  ): BPSession;
  
  // 对比两条路径
  comparePaths(
    pathA: BPSession,
    pathB: BPSession
  ): PathComparison;
  
  // 预测最终结果
  predictOutcome(
    session: BPSession
  ): MatchPrediction;
}

interface PathComparison {
  branchPoint: number;
  originalAction: BPAction;
  alternativeAction: BPAction;
  mapDiffs: MapDiff[];
  predictedWinRates: {
    teamA: { original: number; alternative: number };
    teamB: { original: number; alternative: number };
  };
  keyDifferences: string[];
}
```

---

## 7. 技术选型

### 7.1 前端技术栈

| 技术 | 选型 | 理由 |
|------|------|------|
| 框架 | React 18+ / Next.js 14+ | 生态成熟，SSR 支持 |
| 状态管理 | Zustand + XState | Zustand 轻量状态，XState 管理 BP 状态机 |
| 拖拽 | @dnd-kit | 轻量、无障碍、React 友好 |
| 动画 | Framer Motion | 流畅的过渡动画 |
| 图表 | Recharts / D3.js | 数据可视化 |
| UI 组件 | Tailwind CSS + Radix UI | 快速原型，无障碍 |
| 类型安全 | TypeScript 5+ | 全栈类型安全 |

### 7.2 状态管理架构

```
┌─────────────────────────────────────────────┐
│                  Zustand Store               │
│  ┌───────────────────────────────────────┐  │
│  │  BP State Machine (XState)           │  │
│  │  - currentState                      │  │
│  │  - transition()                      │  │
│  │  - undo()                            │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  Team Store                          │  │
│  │  - teamA, teamB                      │  │
│  │  - stats, history                    │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  Recommendation Store                │  │
│  │  - currentRecommendation             │  │
│  │  - alternatives                      │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  UI Store                            │  │
│  │  - activeTab, modals                 │  │
│  │  - dragState, animations             │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │  What-if Store                       │  │
│  │  - branches                          │  │
│  │  - comparisons                       │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 7.3 XState 状态机配置

```typescript
import { createMachine } from 'xstate';

export const bpMachine = createMachine({
  id: 'bpSimulator',
  initial: 'init',
  states: {
    init: {
      on: { SELECT_FORMAT: 'formatSelect' }
    },
    formatSelect: {
      on: { 
        SELECT_TEAMS: 'teamSelect',
        BACK: 'init'
      }
    },
    teamSelect: {
      on: {
        CONFIRM_TEAMS: 'sideSelect',
        BACK: 'formatSelect'
      }
    },
    sideSelect: {
      on: {
        SET_SIDES: 'banPhase',
        BACK: 'teamSelect'
      }
    },
    banPhase: {
      initial: 'teamABan',
      states: {
        teamABan: { on: { BAN: 'teamBBan' } },
        teamBBan: { on: { BAN: 'pickPhase' } }
      },
      on: {
        BAN: [
          { target: 'pickPhase', cond: 'isPickPhase' },
          { target: 'banPhase' }
        ]
      }
    },
    pickPhase: {
      on: {
        PICK: 'sidePick',
        BACK: 'banPhase'
      }
    },
    sidePick: {
      on: {
        PICK_SIDE: [
          { target: 'pickPhase', cond: 'isNextPick' },
          { target: 'banPhase', cond: 'isNextBan' },
          { target: 'decider', cond: 'isDecider' }
        ]
      }
    },
    decider: {
      on: {
        AUTO_ASSIGN: 'completed',
        PICK_SIDE: 'completed'
      }
    },
    completed: {
      type: 'final',
      on: { RESET: 'init' }
    }
  }
});
```

### 7.4 后端技术选型

| 技术 | 选型 | 理由 |
|------|------|------|
| 运行时 | Node.js 20+ / Bun | 高性能 JS 运行时 |
| 框架 | Hono / Fastify | 轻量、高性能 |
| 数据库 | PostgreSQL + Redis | 持久化 + 缓存 |
| ORM | Drizzle ORM | 类型安全、轻量 |
| API | REST + WebSocket | 实时 BP 同步 |
| 缓存 | Redis | 推荐结果缓存 |

---

## 8. 与第一层数据看板的接口对接

### 8.1 数据流架构

```
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│  第一层:          │     │  第二层:          │     │  第三层:          │
│  数据看板         │────>│  BP 模拟器        │────>│  (后续层)        │
│                  │     │                  │     │                  │
│  - 队伍数据       │     │  - 实时BP模拟     │     │  - 战术分析      │
│  - 地图数据       │     │  - 推荐策略       │     │  - 选手分析      │
│  - 胜率统计       │     │  - 历史回放       │     │  - ...           │
│  - 交手记录       │     │  - What-if       │     │                  │
└──────────────────┘     └──────────────────┘     └──────────────────┘
         │                       │
         └───────────────────────┘
                    │
              ┌─────▼─────┐
              │  共享数据层  │
              │  (API Gateway)│
              └───────────┘
```

### 8.2 API 接口定义

#### 8.2.1 从第一层获取数据

```typescript
// 获取队伍列表及基础数据
GET /api/v1/teams
Response: Team[]

// 获取队伍详细地图数据
GET /api/v1/teams/:teamId/map-stats
Response: MapStats[]

// 获取两队交手记录
GET /api/v1/head-to-head/:teamAId/:teamBId
Response: HeadToHead

// 获取全局地图胜率
GET /api/v1/maps/stats
Response: GlobalMapStats[]

// 获取推荐策略（BP 模拟器专用）
POST /api/v1/recommendation
Body: {
  state: BPState;
  teamA: TeamInfo;
  teamB: TeamInfo;
  availableMaps: MapId[];
  history: BPStateHistory[];
}
Response: StrategyRecommendation
```

#### 8.2.2 BP 模拟器自身 API

```typescript
// 保存 BP 会话
POST /api/v1/bp/sessions
Body: BPSession
Response: { id: string }

// 获取 BP 会话
GET /api/v1/bp/sessions/:sessionId
Response: BPSession

// 获取历史 BP 列表
GET /api/v1/bp/sessions?teamId=xxx&format=BO3&page=1&limit=20
Response: { sessions: BPSession[], total: number }

// 执行 What-if 分析
POST /api/v1/bp/what-if
Body: {
  sessionId: string;
  branchPoint: number;
  alternativeAction: BPAction;
}
Response: PathComparison

// 搜索历史 BP
GET /api/v1/bp/search?q=navi+faze&format=BO3
Response: BPSession[]
```

### 8.3 事件总线（实时同步）

```typescript
// WebSocket 事件
interface BPEvent {
  type: 'BP_STEP' | 'BP_COMPLETE' | 'RECOMMENDATION_UPDATE' | 'WHAT_IF_RESULT';
  sessionId: string;
  payload: any;
  timestamp: number;
}

// 第一层 → 第二层 数据更新事件
interface DataUpdateEvent {
  type: 'TEAM_STATS_UPDATED' | 'MAP_STATS_UPDATED' | 'NEW_MATCH_DATA';
  payload: any;
}
```

---

## 9. 多赛制支持

### 9.1 赛制配置

```typescript
interface FormatConfig {
  type: 'BO1' | 'BO3' | 'BO5';
  totalMaps: number;
  mapsToWin: number;
  
  // 流程步骤定义
  steps: FormatStep[];
  
  // 规则集
  ruleSet: 'valve' | 'traditional';
}

interface FormatStep {
  phase: 'ban' | 'pick' | 'side_pick' | 'decider';
  actor: 'team_a' | 'team_b';
  count: number;  // 禁/选几张图
}
```

### 9.2 各赛制步骤对比

| 步骤 | BO1 (Valve) | BO3 (Valve) | BO5 (传统) |
|------|-------------|-------------|------------|
| 1 | A 禁 2 | A 禁 1 | A 禁 1 |
| 2 | B 禁 3 | B 禁 1 | B 禁 1 |
| 3 | A 禁 1 | A 选 1 | A 选 1 |
| 4 | B 选阵营 | B 选阵营 | B 选阵营 |
| 5 | — | B 选 1 | B 选 1 |
| 6 | — | A 选阵营 | A 选阵营 |
| 7 | — | B 禁 1 | A 选 1 |
| 8 | — | A 禁 1 | B 选阵营 |
| 9 | — | B 选阵营(决胜) | B 选 1 |
| 10 | — | — | A 选阵营 |
| 11 | — | — | 决胜图 |
| 12 | — | — | B 选阵营 |

### 9.3 赛制选择器 UI

```
┌─────────────────────────────────┐
│  选择赛制                        │
│                                 │
│  [BO1]  [BO3 ●]  [BO5]         │
│                                 │
│  规则集:                        │
│  [Valve 官方 ●]  [传统]        │
│                                 │
│  地图池:                        │
│  [Active Duty 2026 ●]          │
│  [自定义]                       │
│                                 │
│  [确认]                         │
└─────────────────────────────────┘
```

---

## 10. 数据模型

### 10.1 核心实体关系

```
┌─────────────┐     ┌─────────────────┐     ┌─────────────┐
│   Team      │     │   BPSession     │     │   Map       │
├─────────────┤     ├─────────────────┤     ├─────────────┤
│  id         │────>│  id             │<────│  id         │
│  name       │     │  format         │     │  name       │
│  logo       │     │  ruleSet        │     │  abbreviation│
│  seed       │     │  teamAId        │     │  active     │
│  region     │     │  teamBId        │     └─────────────┘
└─────────────┘     │  createdAt      │
       │            │  tags           │
       │            └───────┬─────────┘
       │                    │
       │                    ▼
       │            ┌─────────────────┐
       │            │  BPStateHistory │
       │            ├─────────────────┤
       │            │  id             │
       │            │  sessionId      │
       │            │  step           │
       │            │  state          │
       │            │  action         │
       │            │  actor          │
       │            │  mapId          │
       │            │  side           │
       │            │  recommendation │
       │            │  timestamp      │
       │            └─────────────────┘
       │
       ▼
┌─────────────────┐
│  TeamMapStats   │
├─────────────────┤
│  teamId         │
│  mapId          │
│  winRate        │
│  ctWinRate      │
│  tWinRate       │
│  matchesPlayed  │
│  last10Wins     │
│  trend          │
│  updatedAt      │
└─────────────────┘
```

### 10.2 数据库 Schema (Drizzle ORM)

```typescript
import { pgTable, serial, text, integer, timestamp, jsonb, uuid } from 'drizzle-orm/pg-core';

// 队伍表
export const teams = pgTable('teams', {
  id: uuid('id').defaultRandom().primaryKey(),
  name: text('name').notNull(),
  logo: text('logo'),
  seed: integer('seed'),
  region: text('region'),
  createdAt: timestamp('created_at').defaultNow(),
});

// 地图表
export const maps = pgTable('maps', {
  id: text('id').primaryKey(), // 'ancient', 'dust2', etc.
  name: text('name').notNull(),
  abbreviation: text('abbreviation').notNull(),
  active: integer('active').default(1),
});

// BP 会话表
export const bpSessions = pgTable('bp_sessions', {
  id: uuid('id').defaultRandom().primaryKey(),
  format: text('format').notNull(), // 'BO1' | 'BO3' | 'BO5'
  ruleSet: text('rule_set').notNull(), // 'valve' | 'traditional'
  teamAId: uuid('team_a_id').references(() => teams.id),
  teamBId: uuid('team_b_id').references(() => teams.id),
  result: jsonb('result'),
  tags: text('tags').array(),
  createdAt: timestamp('created_at').defaultNow(),
});

// BP 步骤历史表
export const bpHistory = pgTable('bp_history', {
  id: uuid('id').defaultRandom().primaryKey(),
  sessionId: uuid('session_id').references(() => bpSessions.id),
  step: integer('step').notNull(),
  state: text('state').notNull(),
  action: text('action').notNull(), // 'ban' | 'pick' | 'side_pick'
  actor: text('actor').notNull(), // 'team_a' | 'team_b'
  mapId: text('map_id').references(() => maps.id),
  side: text('side'), // 'CT' | 'T'
  recommendation: jsonb('recommendation'),
  timestamp: timestamp('timestamp').defaultNow(),
});

// 队伍地图统计数据
export const teamMapStats = pgTable('team_map_stats', {
  id: uuid('id').defaultRandom().primaryKey(),
  teamId: uuid('team_id').references(() => teams.id),
  mapId: text('map_id').references(() => maps.id),
  winRate: integer('win_rate'), // 0-100
  ctWinRate: integer('ct_win_rate'),
  tWinRate: integer('t_win_rate'),
  matchesPlayed: integer('matches_played'),
  last10Wins: integer('last_10_wins'),
  trend: text('trend'), // 'up' | 'down' | 'stable'
  updatedAt: timestamp('updated_at').defaultNow(),
});
```

---

## 11. API 设计

### 11.1 RESTful API

```
# 队伍
GET    /api/v1/teams                    # 队伍列表
GET    /api/v1/teams/:id                # 队伍详情
GET    /api/v1/teams/:id/map-stats      # 队伍地图数据

# 地图
GET    /api/v1/maps                     # 地图列表
GET    /api/v1/maps/stats               # 全局地图统计

# 交手记录
GET    /api/v1/head-to-head/:teamA/:teamB  # 两队交手记录

# BP 会话
POST   /api/v1/bp/sessions              # 创建 BP 会话
GET    /api/v1/bp/sessions/:id          # 获取 BP 会话
PUT    /api/v1/bp/sessions/:id          # 更新 BP 会话（添加步骤）
DELETE /api/v1/bp/sessions/:id          # 删除 BP 会话
GET    /api/v1/bp/sessions              # BP 会话列表

# 推荐
POST   /api/v1/recommendation           # 获取推荐策略

# What-if
POST   /api/v1/bp/what-if               # What-if 分析
POST   /api/v1/bp/compare               # 对比两条 BP 路径

# 搜索
GET    /api/v1/bp/search?q=xxx          # 搜索历史 BP
```

### 11.2 WebSocket 事件

```typescript
// 客户端 → 服务端
interface ClientEvent {
  type: 'JOIN_SESSION' | 'BP_ACTION' | 'REQUEST_RECOMMENDATION' | 'CREATE_BRANCH';
  sessionId: string;
  payload: any;
}

// 服务端 → 客户端
interface ServerEvent {
  type: 'STATE_UPDATE' | 'RECOMMENDATION' | 'WHAT_IF_RESULT' | 'ERROR';
  sessionId: string;
  payload: any;
}
```

---

## 12. 开发路线图

### Phase 1: 核心引擎 (Week 1-2)
- [ ] BP 状态机实现（XState）
- [ ] 基础 BP 流程（BO3 Valve 规则）
- [ ] 简单的 UI 原型（React + Tailwind）
- [ ] 状态持久化（PostgreSQL schema）

### Phase 2: 交互界面 (Week 3-4)
- [ ] 地图卡片组件（三种状态）
- [ ] 拖拽/点击交互
- [ ] BP 流程时间线组件
- [ ] 赛制选择器
- [ ] 响应式布局

### Phase 3: 推荐引擎 (Week 5-6)
- [ ] 基础推荐算法（基于胜率）
- [ ] 高级推荐算法（多因子）
- [ ] 推荐气泡 UI
- [ ] 数据面板集成

### Phase 4: 历史回放 (Week 7-8)
- [ ] BP 会话存储/读取
- [ ] 回放播放器
- [ ] 历史 BP 搜索
- [ ] 标签/分类系统

### Phase 5: What-if 分析 (Week 9-10)
- [ ] 分支管理
- [ ] 对比视图
- [ ] 结果预测
- [ ] 可视化差异

### Phase 6: 多赛制 & 优化 (Week 11-12)
- [ ] BO1/BO5 支持
- [ ] 传统规则集支持
- [ ] 性能优化
- [ ] 测试覆盖

---

## 附录

### A. 组件树

```
<App>
  <BPProvider>
    <Header>
      <FormatSelector />
      <RuleSetSelector />
    </Header>
    
    <BPMainLayout>
      <TeamPanel side="A">
        <TeamInfo />
        <TeamMapStats />
        <HistoryBPList />
      </TeamPanel>
      
      <MapPool>
        <MapCard /> (×7)
      </MapPool>
      
      <TeamPanel side="B">
        <TeamInfo />
        <TeamMapStats />
        <HistoryBPList />
      </TeamPanel>
    </BPMainLayout>
    
    <BPTimeline>
      <StepNode /> (×N)
      <NavigationControls />
    </BPTimeline>
    
    <BottomPanel>
      <RecommendationPanel>
        <RecommendationCard />
        <AlternativesList />
      </RecommendationPanel>
      
      <DataPanel>
        <MapComparisonChart />
        <WinRateChart />
      </DataPanel>
    </BottomPanel>
    
    <WhatIfModal>
      <BranchSelector />
      <ComparisonView />
    </WhatIfModal>
    
    <ReplayModal>
      <ReplayControls />
      <ReplayTimeline />
    </ReplayModal>
  </BPProvider>
</App>
```

### B. 关键交互流程

```
用户操作流程:
1. 选择赛制 (BO1/BO3/BO5)
2. 选择规则集 (Valve/传统)
3. 选择 Team A 和 Team B
4. 高种子队选边 (或自动)
5. BP 流程开始:
   a. 当前轮到某队
   b. 查看推荐策略
   c. 拖拽/点击地图执行操作
   d. 系统记录步骤，更新状态
   e. 进入下一步
6. BP 完成，显示结果
7. 可选: 保存/分享/回放/What-if

What-if 流程:
1. 在历史 BP 中选择分支点
2. 选择替代操作
3. 系统重新计算后续流程
4. 显示对比结果
5. 可选: 保存分支为新的 BP 会话
```

### C. 关键决策记录 (ADR)

| 决策 | 选项 | 选择 | 理由 |
|------|------|------|------|
| 状态机库 | XState / Zustand | XState | 可视化、可测试、复杂状态管理 |
| 拖拽库 | react-dnd / @dnd-kit / interact.js | @dnd-kit | 轻量、无障碍、活跃维护 |
| 数据库 | PostgreSQL / MongoDB | PostgreSQL | 关系数据、事务支持 |
| 实时通信 | WebSocket / SSE / Polling | WebSocket | 双向通信、低延迟 |

---

> **文档版本:** v1.0  
> **最后更新:** 2026-06-05  
> **作者:** 小龙虾团队  
> **下一阶段:** 实现 Phase 1: 核心引擎
